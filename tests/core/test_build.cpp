// Placeholder proving the test harness is wired up end to end: compiled,
// linked against the module graph, and discovered by ctest. Replaced by real
// core tests once Handle/Arena/CommandStack land.

#include <doctest/doctest.h>

TEST_CASE("test harness is wired up") {
    CHECK(true);
}
